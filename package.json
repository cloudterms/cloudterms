{
	"name": "cloudterms",
	"type": "module",
	"scripts": {
		"build": "turbo build",
		"check": "turbo check",
		"check.fix": "turbo check.fix",
		"clean": "./scripts/clean.sh",
		"dev": "turbo dev",
		"dev.docs": "pnpm --filter docs dev",
		"publish.all": "./scripts/publish-all.sh",
		"update-deps": "pnpm up --latest -r",
		"update-deps.root": "ncu -u && pnpm i",
		"update-package-json-versions": "./scripts/update-package-json-versions.sh"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	},
	"dependencies": {
		"@biomejs/biome": "1.8.3",
		"turbo": "2.0.11"
	},
	"packageManager": "pnpm@9.3.0"
}
