{
  "name": "cloudterms",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build.all": "bun scripts/build-all.ts",
    "publish.all": "bun scripts/publish-all.ts",
    "start.react-vite": "cd examples/react-vite && bun dev"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
