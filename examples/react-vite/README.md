# CloudTerms + React + TypeScript + Vite

This application uses the [Hono API](../hono-api/).

1. Install dependencies

```sh
pnpm install
```

2. Run application

```sh
pnpm dev
```

3. Open http://localhost:3001
