{
	"name": "@cloudterms/hono-api-example",
	"type": "module",
	"scripts": {
		"check": "tsc --noEmit && biome check .",
		"check.fix": "tsc --noEmit && biome check --write .",
		"dev": "PORT=3002 pnpm with-env tsx watch src/index.ts",
		"with-env": "dotenv -e ../../.env -c --"
	},
	"dependencies": {
		"@cloudterms/js": "workspace:*",
		"@hono/node-server": "^1.12.2",
		"dotenv-cli": "7.4.2",
		"hono": "^4.5.10"
	},
	"devDependencies": {
		"@types/node": "22.5.1",
		"tsx": "^4.19.0"
	}
}
