{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**"]
		},
		"check": { "cache": false },
		"check.fix": { "cache": false },
		"clean": { "cache": false },
		"dev": {
			"cache": false,
			"persistent": true
		}
	}
}
